# Deprecated backend folder

This folder is kept only for legacy reference.

## Active backend
Use the backend at workspace root:
- server: `server.js`
- package: `package.json`

Do not run `npm install` or `npm start` from this folder.
